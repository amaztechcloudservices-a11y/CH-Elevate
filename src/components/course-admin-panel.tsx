"use client";

import { CourseMaterialUpload } from "./course-material-upload";
import { CourseInvoiceUpload } from "./course-invoice-upload";
import { CourseRosterUpload } from "./course-roster-upload";
import { CourseCatalogueAdmin } from "./course-catalogue-admin";

import { Award, CalendarDays, FileUp, GraduationCap, LoaderCircle, RefreshCw, Users } from "lucide-react";
import { FormEvent, useEffect, useMemo, useRef, useState } from "react";

import { formatMoney } from "@/lib/courses";
import { StudentPasswordReset } from "@/components/student-password-reset";
import { CourseAccessEmail } from "@/components/course-access-email";
import { CourseCurriculumAdmin } from "@/components/course-curriculum-admin";
import { CourseStudentsAdmin } from "@/components/course-students-admin";
import { CourseParticipantReports } from "@/components/course-participant-reports";
import { CourseAnalyticsPanel } from "@/components/course-analytics-panel";

type Course = { id: string; title: string; slug: string; summary: string; description: string; isActive: boolean };
type OfferingRow = { offering: { id: string; courseId: string; code: string; startsAt: string; endsAt: string; deliveryMode: string; venue: string | null; joiningInstructions: string | null; feeCents: number; currency: string; capacityMode: string; capacity: number | null; registrationOpensAt: string | null; registrationClosesAt: string | null; substitutionCutoffAt: string | null; isPublished: boolean; isCancelled: boolean; updatedAt: string }; courseTitle: string; approvedSeats: number };
type RegistrationRow = { participantProfileId?: string | null; courseId?: string; offeringId?: string; currency: string; registration: { organisationId?: string | null; applicantName?: string; applicantEmail?: string; id: string; status: string; paymentStatus: string; paymentReference: string | null; amountDueCents: number }; courseTitle: string; offeringCode: string; startsAt: string; organisationName: string | null; participantId: string; participantName: string; participantEmail: string; participantStatus: string; attendance: string; completedAt: string | null };
type MaterialRow = { recipientName?: string | null; recipientEmail?: string | null; material: { id: string; title: string; courseId: string | null; offeringId: string | null; originalFilename: string; version: number; isArchived: boolean }; courseTitle: string | null; offeringCode: string | null };
type AdminData = { courses: Course[]; offerings: OfferingRow[]; registrations: RegistrationRow[]; materials: MaterialRow[]; recentActivity: { id: string; action: string; entityType: string; entityId: string | null; createdAt: string }[]; metrics: { pending: number; upcoming: number; waitlisted: number; outstandingCents: number } };

function Status({ value }: { value: string }) { return <span className={`course-status course-status--${value}`}>{value.replaceAll("_", " ")}</span>; }
function jamaicaDate(value: FormDataEntryValue | null) { return value ? `${String(value)}-05:00` : null; }
function jamaicaInput(value: string | null) { if (!value) return ""; return new Intl.DateTimeFormat("sv-SE", { timeZone: "America/Jamaica", year: "numeric", month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit", hour12: false }).format(new Date(value)).replace(" ", "T"); }

export function CourseAdminPanel() {
  const [data, setData] = useState<AdminData | null>(null); const [message, setMessage] = useState("Loading course administration…"); const [view, setView] = useState<"registrations" | "courses" | "materials" | "curriculum" | "students" | "reports" | "analytics">("registrations"); const [selectedRegistrations, setSelectedRegistrations] = useState<string[]>([]); const [selectedOfferingId, setSelectedOfferingId] = useState("");
  const [offeringDraft, setOfferingDraft] = useState<OfferingRow["offering"] | null>(null);
  const [offeringBusy, setOfferingBusy] = useState(false);
  const offeringInFlight = useRef(false);
  async function load() {
    try {
      const response = await fetch("/api/admin/courses", { cache: "no-store" });
      const result = await response.json() as { data?: AdminData; error?: { message?: string } };
      if (!response.ok || !result.data) { setMessage(result.error?.message || "Course administration could not be loaded."); return false; }
      setData(result.data); setMessage(""); return true;
    } catch { setMessage("Course administration could not be refreshed. Your current records are still shown."); return false; }
  }
  useEffect(() => { const controller = new AbortController(); fetch("/api/admin/courses", { cache: "no-store", signal: controller.signal }).then(async (response) => { const result = await response.json() as { data?: AdminData; error?: { message?: string } }; if (controller.signal.aborted) return; if (!response.ok || !result.data) throw new Error(result.error?.message || "Course administration could not be loaded."); setData(result.data); setMessage(""); }).catch((error) => { if (!controller.signal.aborted) setMessage(error instanceof Error ? error.message : "Course administration could not be loaded."); }); return () => controller.abort(); }, []);
  async function create(event: FormEvent<HTMLFormElement>, kind: "offering") {
    event.preventDefault();
    if (offeringInFlight.current) return;
    const formElement = event.currentTarget;
    const values = Object.fromEntries(new FormData(formElement).entries());
    const body = { kind, courseId: values.courseId, code: values.code, deliveryMode: values.deliveryMode, venue: values.venue, joiningInstructions: values.joiningInstructions, currency: values.currency, capacityMode: values.capacityMode, startsAt: jamaicaDate(values.startsAt), endsAt: jamaicaDate(values.endsAt), feeCents: Math.round(Number(values.fee) * 100), capacity: values.capacityMode === "unlimited" ? null : Number(values.capacity), registrationOpensAt: jamaicaDate(values.registrationOpensAt), registrationClosesAt: jamaicaDate(values.registrationClosesAt), substitutionCutoffAt: jamaicaDate(values.substitutionCutoffAt), isPublished: values.isPublished === "on" };
    offeringInFlight.current = true; setOfferingBusy(true); setMessage("");
    try {
      const response = await fetch("/api/admin/courses", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) });
      const result = await response.json() as { error?: { message?: string } };
      if (!response.ok) { setMessage(result.error?.message || "Offering could not be created."); return; }
      formElement.reset();
      setMessage(await load() ? "Offering scheduled." : "Offering scheduled, but the list could not be refreshed. Refresh before creating another offering.");
    } catch { setMessage("The result could not be confirmed. Your draft is still here. Refresh and check the offering code before retrying."); }
    finally { offeringInFlight.current = false; setOfferingBusy(false); }
  }
  async function update(body: Record<string, unknown>) {
    try {
      const response = await fetch("/api/admin/courses", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) });
      const result = await response.json() as { message?: string; error?: { message?: string } };
      if (!response.ok) { setMessage(result.error?.message || "Update failed."); return false; }
      const refreshed = await load();
      setMessage([result.message || "Update saved.", ...(!refreshed ? ["The list could not be refreshed. Refresh before making another change."] : [])].join(" "));
      return true;
    } catch { setMessage("The result could not be confirmed. Your draft is still here. Refresh and check the record before retrying."); return false; }
  }
  async function updateOffering(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    if (offeringInFlight.current || !offeringDraft) return;
    const form = new FormData(event.currentTarget), offering = offeringDraft;
    offeringInFlight.current = true; setOfferingBusy(true);
    try {
      const saved = await update({ action: "offering_update", id: offering.id, updatedAt: offering.updatedAt, startsAt: jamaicaDate(form.get("startsAt")), endsAt: jamaicaDate(form.get("endsAt")), deliveryMode: offering.deliveryMode, venue: form.get("venue"), joiningInstructions: form.get("joiningInstructions"), feeCents: offering.feeCents, currency: offering.currency, capacityMode: offering.capacityMode, capacity: offering.capacity, registrationOpensAt: offering.registrationOpensAt, registrationClosesAt: offering.registrationClosesAt, substitutionCutoffAt: offering.substitutionCutoffAt, isPublished: offering.isPublished });
      if (saved) { setSelectedOfferingId(""); setOfferingDraft(null); }
    } finally { offeringInFlight.current = false; setOfferingBusy(false); }
  }
  async function bulkApprove() { if (!selectedRegistrations.length) return; if (await update({ action: "bulk_registration_status", ids: selectedRegistrations, status: "approved", overrideCapacity: false })) setSelectedRegistrations([]); }
  const grouped = useMemo(() => data ? [...new Map(data.registrations.map((row) => [row.registration.id, row])).values()] : [], [data]);
  if (!data) return <div className="course-admin-loading"><LoaderCircle className="spin" /><p>{message}</p></div>;
  const selectedOffering = offeringDraft;
  return <div className="course-admin"><header className="cms-panel-heading"><span>Course operations</span><h1>Courses & registration.</h1><p>Schedule offerings, approve participants, assign secure materials, track payment and attendance, and issue certificates.</p></header>{message && <p className="cms-admin__notice cms-admin__notice--success" role="status">{message}</p>}
    {view === "registrations" && <StudentPasswordReset participants={data.registrations} />}
    {view === "registrations" && <CourseAccessEmail registrations={data.registrations} />}
    <div className="cms-metrics"><button onClick={() => setView("registrations")}><span>Pending applications</span><strong>{data.metrics.pending}</strong></button><button onClick={() => setView("courses")}><span>Upcoming courses</span><strong>{data.metrics.upcoming}</strong></button><button onClick={() => setView("registrations")}><span>Waitlisted</span><strong>{data.metrics.waitlisted}</strong></button><button onClick={() => setView("registrations")}><span>Outstanding</span><strong>{formatMoney(data.metrics.outstandingCents)}</strong></button></div>
    <div className="course-admin__tabs"><button className={view === "registrations" ? "active" : ""} onClick={() => setView("registrations")}><Users /> Registrations</button><button className={view === "courses" ? "active" : ""} onClick={() => setView("courses")}><GraduationCap /> Courses & offerings</button><button className={view === "materials" ? "active" : ""} onClick={() => setView("materials")}><FileUp /> Materials</button><button className={view === "curriculum" ? "active" : ""} onClick={() => setView("curriculum")}>Modules &amp; lessons</button><button className={view === "students" ? "active" : ""} onClick={() => setView("students")}>Student profiles</button><button className={view === "reports" ? "active" : ""} onClick={() => setView("reports")}>Reports</button><button className={view === "analytics" ? "active" : ""} onClick={() => setView("analytics")}>Analytics</button><button onClick={load}><RefreshCw /> Refresh</button></div>
    <div hidden={view !== "curriculum"}><CourseCurriculumAdmin courses={data.courses} /></div>
    {view === "students" && <CourseStudentsAdmin />}
    {view === "reports" && <CourseParticipantReports courses={data.courses} registrations={data.registrations} />}
    {view === "analytics" && <CourseAnalyticsPanel courses={data.courses} />}
    {view === "registrations" && <section className="cms-card cms-table-wrap"><div className="cms-card__heading"><h2>Registrations</h2><button className="course-table-action" disabled={!selectedRegistrations.length} onClick={bulkApprove}>Approve selected ({selectedRegistrations.length})</button></div><table><thead><tr><th><span className="sr-only">Select</span></th><th>Participant</th><th>Course</th><th>Organisation</th><th>Registration</th><th>Payment</th><th>Attendance</th><th>Certificate</th></tr></thead><tbody>{data.registrations.map((row) => <tr key={row.participantId}><td><input type="checkbox" aria-label={`Select registration for ${row.participantName}`} checked={selectedRegistrations.includes(row.registration.id)} onChange={(event) => setSelectedRegistrations(event.target.checked ? [...new Set([...selectedRegistrations, row.registration.id])] : selectedRegistrations.filter((id) => id !== row.registration.id))} /></td><td><strong>{row.participantName}</strong><span>{row.participantEmail}</span></td><td><strong>{row.courseTitle}</strong><span>{row.offeringCode} · {new Date(row.startsAt).toLocaleDateString("en-JM", { timeZone: "America/Jamaica" })}</span></td><td>{row.organisationName || "Individual"}</td><td><Status value={row.participantStatus} /><select aria-label={`Registration status for ${row.participantName}`} value={row.participantStatus} onChange={(event) => update({ action: "registration_status", id: row.registration.id, participantId: row.participantId, status: event.target.value, overrideCapacity: false })}><option value="pending_review" disabled>Pending review</option><option value="approved">Approve</option><option value="waitlisted">Waitlist</option><option value="rejected">Reject</option><option value="cancelled">Cancel</option><option value="completed">Complete</option></select>{row.participantStatus === "waitlisted" && <button className="course-table-action" onClick={() => update({ action: "registration_status", id: row.registration.id, participantId: row.participantId, status: "approved", overrideCapacity: true })}>Override offering capacity</button>}</td><td><Status value={row.registration.paymentStatus} /><select aria-label={`Payment status for ${row.participantName}`} value={row.registration.paymentStatus} onChange={(event) => update({ action: "payment", id: row.registration.id, paymentStatus: event.target.value, paymentReference: row.registration.paymentReference || "" })}>{["unpaid", "invoiced", "partially_paid", "paid", "waived", "refunded"].map((value) => <option key={value}>{value}</option>)}</select></td><td><select aria-label={`Attendance for ${row.participantName}`} value={row.attendance} onChange={(event) => update({ action: "attendance", participantIds: [row.participantId], attendance: event.target.value, complete: event.target.value === "attended" })}>{["not_recorded", "attended", "partially_attended", "no_show"].map((value) => <option key={value} value={value}>{value.replaceAll("_", " ")}</option>)}</select></td><td>{row.participantStatus === "completed" && row.attendance === "attended" && row.completedAt ? <button className="course-table-action" onClick={() => update({ action: "certificate", participantId: row.participantId })}><Award /> Issue</button> : "—"}</td></tr>)}</tbody></table>{!data.registrations.length && <p className="cms-empty">No course registrations yet.</p>}</section>}
    {view === "registrations" && <div className="course-admin__forms"><CourseInvoiceUpload registrations={grouped} refresh={load} onMessage={setMessage} /><CourseRosterUpload offerings={data.offerings} refresh={load} onMessage={setMessage} /></div>}
    {view === "registrations" && <section className="cms-card course-activity"><div className="cms-card__heading"><h2>Recent course activity</h2></div>{data.recentActivity.length ? <ul>{data.recentActivity.map((item) => <li key={item.id}><strong>{item.action.replaceAll("course.", "").replaceAll("_", " ")}</strong><span>{item.entityType.replaceAll("_", " ")} · {new Date(item.createdAt).toLocaleString("en-JM", { timeZone: "America/Jamaica" })}</span></li>)}</ul> : <p className="cms-empty">No course activity has been recorded yet.</p>}</section>}
    {view === "courses" && <><CourseCatalogueAdmin onChanged={async () => { await load(); }} /><div className="course-admin__forms"><form className="cms-card course-admin-form" onSubmit={(event) => create(event, "offering")}><div className="cms-card__heading"><h2><CalendarDays /> Schedule offering</h2></div><label><span>Course</span><select name="courseId" required><option value="">Select course</option>{data.courses.map((course) => <option key={course.id} value={course.id}>{course.title}</option>)}</select></label><div className="course-form-grid"><label><span>Offering code</span><input name="code" required /></label><label><span>Delivery</span><select name="deliveryMode"><option value="in_person">In person</option><option value="virtual">Virtual</option><option value="blended">Blended</option></select></label><label><span>Starts (Jamaica time)</span><input name="startsAt" type="datetime-local" required /></label><label><span>Ends (Jamaica time)</span><input name="endsAt" type="datetime-local" required /></label><label><span>Venue or platform</span><input name="venue" /></label><label><span>Fee (JMD)</span><input name="fee" type="number" min="0" max="21474836.47" step="0.01" defaultValue="0" required /></label><label><span>Capacity mode</span><select name="capacityMode"><option value="hard">Hard cap + waitlist</option><option value="soft">Soft cap</option><option value="unlimited">Unlimited</option></select></label><label><span>Capacity</span><input name="capacity" type="number" min="1" defaultValue="20" /></label><label><span>Registration opens</span><input name="registrationOpensAt" type="datetime-local" /></label><label><span>Registration closes</span><input name="registrationClosesAt" type="datetime-local" /></label><label><span>Substitution cutoff</span><input name="substitutionCutoffAt" type="datetime-local" /></label></div><label><span>Joining instructions</span><textarea name="joiningInstructions" /></label><label className="course-check"><input name="isPublished" type="checkbox" /> Publish in course catalogue</label><input name="currency" type="hidden" value="JMD" /><button type="submit" disabled={offeringBusy}>{offeringBusy ? "Saving…" : "Schedule offering"}</button></form><form className="cms-card course-admin-form" onSubmit={updateOffering}><div className="cms-card__heading"><h2><CalendarDays /> Update schedule</h2></div><label><span>Offering</span><select name="id" value={selectedOfferingId} disabled={offeringBusy} onChange={(event) => { setSelectedOfferingId(event.target.value); setOfferingDraft(data.offerings.find((row) => row.offering.id === event.target.value)?.offering || null); }} required><option value="">Select offering</option>{data.offerings.filter((row) => !row.offering.isCancelled).map((row) => <option key={row.offering.id} value={row.offering.id}>{row.courseTitle} · {row.offering.code}</option>)}</select></label>{selectedOffering && <div key={selectedOffering.id}><label><span>Starts (Jamaica time)</span><input name="startsAt" type="datetime-local" defaultValue={jamaicaInput(selectedOffering.startsAt)} required /></label><label><span>Ends (Jamaica time)</span><input name="endsAt" type="datetime-local" defaultValue={jamaicaInput(selectedOffering.endsAt)} required /></label><label><span>Venue or platform</span><input name="venue" defaultValue={selectedOffering.venue || ""} /></label><label><span>Joining instructions</span><textarea name="joiningInstructions" defaultValue={selectedOffering.joiningInstructions || ""} /></label></div>}<button type="submit" disabled={!selectedOffering || offeringBusy}>Save schedule and notify</button></form></div><section className="cms-card cms-table-wrap"><div className="cms-card__heading"><h2>Course catalogue</h2></div><table><thead><tr><th>Course / offering</th><th>Date</th><th>Seats</th><th>Public status</th></tr></thead><tbody>{data.offerings.map((row) => <tr key={row.offering.id}><td><strong>{row.courseTitle}</strong><span>{row.offering.code}</span></td><td>{new Date(row.offering.startsAt).toLocaleString("en-JM", { dateStyle: "medium", timeStyle: "short", timeZone: "America/Jamaica" })}</td><td>{row.approvedSeats}{row.offering.capacity ? ` / ${row.offering.capacity}` : ""}</td><td>{row.offering.isCancelled ? <Status value="cancelled" /> : <><button className="course-table-action" onClick={() => update({ action: "offering_published", id: row.offering.id, published: !row.offering.isPublished })}>{row.offering.isPublished ? "Unpublish" : "Publish"}</button><button className="course-table-action" onClick={() => { if (window.confirm(`Cancel ${row.offering.code} and notify registered applicants?`)) update({ action: "offering_cancel", id: row.offering.id }); }}>Cancel offering</button></>}</td></tr>)}</tbody></table></section></>}
    {view === "materials" && <div className="course-admin__materials"><CourseMaterialUpload courses={data.courses} offerings={data.offerings} registrations={data.registrations} onUploaded={async () => { await load(); }} /><section className="cms-card cms-table-wrap"><div className="cms-card__heading"><h2>Assigned materials</h2></div><table><thead><tr><th>Material</th><th>Course</th><th>Offering / student</th><th>Version</th><th>Status</th></tr></thead><tbody>{data.materials.map((row) => <tr key={row.material.id}><td><strong>{row.material.title}</strong><span>{row.material.originalFilename}</span></td><td>{row.courseTitle}</td><td>{row.offeringCode || "All offerings"}{row.recipientName && <span>{row.recipientName} · {row.recipientEmail}</span>}</td><td>v{row.material.version}</td><td><button className="course-table-action" onClick={() => update({ action: "archive_material", id: row.material.id, archived: !row.material.isArchived })}>{row.material.isArchived ? "Restore" : "Archive"}</button></td></tr>)}</tbody></table></section></div>}
  </div>;
}
