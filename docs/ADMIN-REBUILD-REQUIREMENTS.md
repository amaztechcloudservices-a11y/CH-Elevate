# Administration rebuild — requirements and evidence

Status: functional build and local release gates complete. The only external content dependency is the real workshop title/date/image/copy for C2. Local review only; no deployment, commit, or push authorized for this rebuild.

## Authoritative inputs

- `D:/CLARHEN CONSULTING/Booking System and Course Registration Build Requirements.txt`
- `D:/CLARHEN CONSULTING/Course Builder Fields.docx`
- User clarification, 2026-09-03: include modules and lessons; no quizzes; subscription field only, no payment gateway.
- User additions, 2026-09-03: admins upload course materials for authenticated student downloads; students have a one-page profile where admins can post information; admins can reset forgotten student passwords.
- Existing registration, approvals, private resources, offline payment tracking, organisation coordination, attendance, and certificates must be retained.

## Delivery sequence

1. Separate workspaces and links, isolate reads/writes, retain login credentials and old bookmarks.
2. Booking-event data model, event CRUD, custom fields, agent identity and event-specific availability.
3. Full booking calendar, request management, two-step public booking and configurable email templates.
4. Course catalogue CRUD/modal, public cards and registration, participant PDF and analytics/reports.
5. Modules/lessons, access/subscription metadata without gateways or quizzes.
6. Full data-backed acceptance tests, negative authorization tests, responsive review, production build, and requirement-by-requirement audit.

## Acceptance ledger

Unchecked requirements are not complete merely because related legacy functionality exists.

- [x] Separate Booking, Course Registration, Website Management workspaces with appropriate sidebars, top-bar switching, correctly labelled logins, same admin account, and preserved bookmarks. Real credential login/switching and responsive sidebar/navigation tests pass.
- [x] Website content saves cannot overwrite Booking or Course settings. Each workspace loads only its own operational APIs. Website-only GET/PATCH contracts, actual database preservation checks and live workspace request inventories verify the boundary.
- [x] All workspace page titles retain 27px, weight 500, #005751; descriptions #2c2d30. Every sidebar destination, including booking email settings, passes the shared heading checks; global panel checks cover five widths.
- [x] B1: Create and persist booking events. Verified with actual signed-in browser creation, reload and PostgreSQL readback; fixture rows removed afterward.
- [x] B2: Edit, duplicate, delete booking events and appointments safely. Linked events cannot be deleted; appointment deletion is confirmed, recoverable and audited. Fixture browser and PostgreSQL tests cover the operations and conflict failures.
- [x] B3: Published booking events selectable from discovery-call CTA. `/book` lists published events and supports `?event=slug`; actual browser publish/unpublish visibility verified.
- [x] B4: Full-size month calendar shows bookings and event-specific available dates/times. Appointments have edit/delete controls; date windows can be added/removed, dates closed, or weekly hours restored. Signed-in local browser and database readback verify persistence.
- [x] B5: Availability horizon configurable in days, weeks, months, years, or indefinitely, with timezone-aware dates and collision checks. Real-session editor saves/reload/readback and public calendar boundary checks pass for all five units; isolated database tests verify booking enforcement and collisions.
- [x] B6: Paginated complete request list, approval/rejection and consistent public availability. Verified beyond 250 records, including competing reservations, stale edits, rejected slots and restoration conflicts. Customer notifications remain B11.
- [x] B7: Session durations 30, 45, 60, 90 minutes. Shared schema restricts durations; admin editor and public persisted booking duration verified.
- [x] B8: Top-bar workspace switches in each area (covered by workspace acceptance above).
- [x] B9: Custom event questionnaire fields can be added/removed; server validates responses against the selected event. All four field types, edits/removal, required/optional answers, invalid choices/types/lengths and stored historical labels are verified.
- [x] B10: Two-step public booking: title/subtitle/description, assigned agent photo, month navigation/date/time; then questionnaire; persisted booking and confirmation. Browser-to-handler/database tests verify saved answers, transport retry, a competing reservation, retained inputs and final confirmation.
- [x] B11: Configurable booking notification/confirmation sender and templates, safe variable interpolation, verified email delivery using local capture in tests (no unintended real recipients). Local SMTP capture verifies receipt, approval, retries and failure reporting; external inbox delivery is not claimed.
- [x] C1: Site registration persists participants and generates course-specific lists. The PostgreSQL-backed public submission test verifies participant persistence and the protected administration list; responsive UI tests verify form submission. Selected-participant PDF output is still C4b.
- [x] C2: Course administration can publish or unpublish the catalogue as a new section on any main website page, choose a color or image background, and present all published courses in a single horizontal carousel with Register, View, and Share actions.
- [x] C3: Course cards have title/subtitle/description/banner/registration button and view/edit/duplicate/delete controls; published courses appear on Programmes. Registration is disabled until an open scheduled offering exists. Linked offerings/materials prevent hard deletion; archive preserves their records.
- [x] C4a: Add New Course opens accessible modal with required catalogue fields and validation. Five-width interaction tests and signed-in creation/reload/public-visibility/database readback passed.
- [x] C4b: Public registration opens modal and stores participant records; admin can bulk-select a course's participants and generate an accurate PDF. Real-session export verifies individual selection within a group, course isolation, stored data and private attachment headers; extracted PDF text proves selected names and exclusion of unselected/private fields. Pagination and five-width interactions are verified.
- [x] C5: Course analytics, graphs, statistics and downloadable reports are backed by stored data and clear filters.
- [x] F1: Course identity: title, unique slug, summary, full description, instructor reference, subtitle and banner. Instructor is a foreign key to an active staff/admin profile; publication validates required card identity and category.
- [x] F2: Ordered modules and lessons with text, HTTPS video links and private course-wide downloads. Draft/published controls, strict content validation, protected personal-enrolment access and stale-save conflicts are verified. No quizzes, assignments, SCORM or automatic grading.
- [x] F3: Access metadata, price/currency and subscription field. All four access types and five currencies persist with exact cents; private courses are excluded from public listings and direct registration. Subscription notes appear publicly only for subscription access. Offering fees remain independent and approval/payment status remains manual. No online gateway, checkout or automatic charging.
- [x] F4: Category, skill level, draft/published/archived status and enrolment limit (unlimited supported). Catalogue validation/persistence and course-wide approval enforcement are verified, including concurrent approvals and limit edits.
- [x] S1: Admin material upload, course/offering/individual-student assignment and authenticated authorized download from the student's one-page profile. Signed-in browser and PostgreSQL tests verify persistence, content, archive/restore, approval revocation, cross-student/coordinator denial and atomic versions. Private paths are not exposed by upload or student responses.
- [x] S2: One-page student profile aggregates enrolment, resources and administrator-posted information. Admin create/edit/publish/remove, student login/profile update/sign-out, draft privacy and cross-student denial are verified with real sessions and PostgreSQL; five-width interaction tests pass.
- [x] S3: Administrator-triggered student password reset through an expiring reset link to the student's registered email; no disclosure of passwords, audit action, session/authorization safeguards and negative tests. Real Better Auth sessions, isolated PostgreSQL tables and loopback SMTP capture verify delivery, concurrent-request cooldown, single-use/expiry and revoked sessions. External inbox delivery is not claimed.
- [x] Retained portal flows: registration/approval, private materials, offline invoices/payment status, attendance, certificates, organisation rosters and coordination.
  - [x] Attendance/certificate lifecycle: completion/correction, idempotent issuance, current eligibility, student-only downloads, public verification, embedded-font PDF and admin controls verified locally as detailed below.
  - [x] Organisation CSV roster imports: bounded/validated uploads, quoted-field parsing, atomic pending registrations, duplicate/cancelled-offering rejection and admin browser retry/reload verified locally.
  - [x] Invitation acceptance boundary: current approved registration/participant ownership, active student roles, email ownership proof, one-time transactional acceptance and activation retry verified locally. Delivery/recovery remains separate below.
  - [x] Coordinator participant replacement and individual admin review: versioned/transactional changes, cutoff/role checks, revoked old invitations/certificates, cleared attendance/completion, unaffected group members and capacity-aware review verified locally.
  - [x] Admin access-email resend and truthful decision-delivery warnings: recipient-bound renewal, cooldown, token revocation, unchanged decisions, failure/retry and responsive browser-to-database checks pass locally. External inbox delivery is not claimed.
  - [x] Real-auth invitation journey: public registration, real-session approval, captured SMTP invitation/resend, browser signup or existing-account sign-in, one-time acceptance, email verification and profile/lesson reload pass locally. Lost-success-response recovery reaches the profile through sign-in without accepting the token twice; coordinator access does not grant personal learning enrolment.
  - [x] Offline invoice/receipt/payment policy and additional-seat billing, including failure and authorization paths. Evidence uploads do not change payment state or registration totals; payment status remains an explicit admin action. Adding a seat atomically adds the current offering fee and adjusts paid/invoiced/refunded states conservatively; replacements do not rebill.
- [x] Final checks: data survives reload/restart; unauthorized reads/writes denied; invalid data rejected; no secrets in source/tests; no production mutation; complete relevant unit/E2E/build and responsive gates.
  - [x] Website inbox mutation boundary: same-origin strict/versioned updates, missing/excluded-record errors, transactional audit, failure feedback and refresh/retry are verified locally. Booking submissions are excluded from both reads and updates.
  - [x] Public booking and course-registration writes have bounded streamed JSON bodies and atomic PostgreSQL rate limits using hashed identities; spoofable forwarding headers are not trusted.
  - [x] Better Auth 1.7 account identity migration is applied locally with required issuer values and a unique `(issuer, account_id)` key. Credential activation/sign-in and administrator-triggered password reset pass with real auth sessions.
  - [x] Final local evidence: 169/169 tests across 42 files, TypeScript/ESLint with zero errors, clean diff check, zero leftover test schemas, zero known production dependency vulnerabilities, successful 81-page/route production build, and HTTP 200 smoke checks for home, health, booking, programmes, admin login and student login on loopback port 3001.

## Current foundation (not full acceptance)

Dedicated `/admin/bookings`, `/admin/courses`, `/admin/website` routes preserve legacy redirects and provide workspace-specific navigation and reads. The verified workspace boundary is described below. Remaining feature-level acceptance is tracked explicitly in the unchecked ledger items rather than inferred from the existence of an editor.

### Verified workspace boundary (local only)

- Website Management now reads/writes only global website settings, hero slides, page content and non-booking forms. `/api/admin/cms` uses a strict website-only contract: it neither returns availability nor accepts mixed booking payloads. Website saves preserve the hidden legacy booking form from the locked server-side record, never from browser input. Booking events, availability, unrelated CMS records and course data remain untouched. Public/seed full-snapshot compatibility remains separate and unchanged.
- The Website API selects only its own document keys. Invalid stored website content fails closed instead of returning defaults that could overwrite it on a subsequent save. Publication is transactional and audited without content bodies; forms preservation uses a database lock. CMS and legacy booking-settings writes require same origin and strict top-level input. Website saves retain the draft and restore the save button after connection failure. Existing open tabs carrying the old full snapshot must reload before publishing.
- Four isolated PostgreSQL integration tests verify read/write shape, mixed-payload rejection, role/origin restrictions, invalid stored records, preserved legacy forms, and byte-equivalent booking/course/other-document records including timestamps. One drives the actual Next editor into actual handlers and isolated tables, including failed transport, retry and reload. That test fixtures authorization, not a real production user. No live website content is changed by these tests.
- A separate real-session browser test creates a temporary local account, proves anonymous/customer/inactive-admin denial, signs in through the Booking login form and switches through all three workspaces without another login. It verifies login labels, actual operational API requests, website response shape, cross-origin and mixed-contract rejection; it performs no successful content writes. The account is removed in an afterEach hook.
- The 30-test workspace/typography regression passed; a final two-test run additionally verifies the email sidebar heading and expanded real-session negative checks. All 91 unit/database tests across 28 files, TypeScript/lint and the 80-route build passed. Existing locally bundled fonts and responsive styling are retained. No migration, dependency, external email, deployment, commit or push was introduced.
- Rollback keeps all stored documents/data and reverts the website client/API together; do not reintroduce a full-snapshot browser writer that can modify booking settings. Final retained-flow/security acceptance remains separate, including legacy Website inbox mutation error/origin handling and other unchecked requirements.

### Booking event workflow progress (local only)

- Added migration `0005_booking_events`: booking-event definitions and an optional event reference on appointments. Applied only after verifying `localhost:55434/premium_web`. Existing appointments retained. The FK restricts deleting linked events.
- Booking events now own weekly hours, date closures/overrides, durations, lead time, timezone, horizon, agent identity/photo and custom questions. Drafts require explicit publication. Duplicate creates an unpublished copy; stale edits and duplicate slugs are rejected.
- Public `/book` and the contact-page booking form use event selection, agent summary, month navigation, available slots, then the questionnaire. New requests no longer read Website CMS forms or add booking entries to Website's inbox. Legacy CMS booking configuration remains stored but is no longer exposed in Website's form editor; old availability bookmarks open event settings.
- All events currently share one consultation resource: any occupied appointment or explicit block prevents overlapping requests across event types. Multiple simultaneous consultant calendars are not claimed. A transaction-level lock prevents simultaneous request collisions.
- Verification: PostgreSQL-backed route test covers CRUD, publication filtering, stale edits, custom required answers, durations, concurrent requests, occupied slots and linked-delete refusal; mail is captured in-process, never sent to real recipients. Actual signed-in browser test verifies customer/admin boundaries, UI creation, persistence, public visibility and unpublishing with a temporary account. Browser fixtures exercise both steps and CRUD at 320, 375, 768, 1024 and 1440px. Shared-workspace/typography checks also pass.
- Production build passed during this increment; final full-scope acceptance remains pending. No production deployment or data writes. There are no fabricated live events: an administrator must configure and publish a real event before clients can book locally.
- Calendar and appointment work below covers B2/B4/B6; booking communications covers B11, availability horizons cover B5, and the questionnaire/two-step acceptance below covers B9/B10. Remaining course/retained-flow and final release requirements are tracked in the unchecked ledger entries.

### Booking calendar and request management progress (local only)

- Added migration `0006_booking_calendar_actions`: `rejected` appointment status and nullable `deleted_at`. Applied only to verified `localhost:55434/premium_web`; existing rows were retained.
- The Bookings page now has Calendar, All requests and Deleted bookings views. The calendar is month-bounded without a booking-count cutoff; the searchable/status-filtered request list is paginated at 50 records and covers historical records beyond the old 250-row limit.
- Calendar event selection changes the availability overlay/timezone, not the scope of visible bookings. Date windows can be added/removed, closed or reverted to weekly hours; editing event hours never deletes an existing appointment. Legacy global blocks are displayed and respected, but do not yet have their own editor.
- Native accessible booking dialog supports questionnaire/contact details, private admin notes, approve/reject/status changes, reschedule, duplicate, confirmed soft-delete and restore. New scheduled times are explicitly labelled administrator overrides of public hours/notice; they must be in the future and cannot overlap any reserved appointment or block. Rescheduled/duplicate appointments are pending. Restores keep the prior status and reject occupied times.
- Every mutation requires active client-admin access, same-origin headers, strict allowlisted input, current record version and an audit action. Public requests and administrative reservation changes share the same PostgreSQL advisory lock. Public slot calculations now exclude rejected, cancelled and soft-deleted bookings consistently.
- Verification: 5 new unit tests; 2 new PostgreSQL-backed integration tests covering transitions, conflict races with public requests, stale edits, blocks, pagination and recovery; actual signed-in browser verifies customer/admin boundaries and UI persistence for approve/edit/delete/restore and date closure. Fixtures are uniquely identified and removed; mail is captured for public test requests, and no real customer email is sent.
- Responsive interaction tests pass at 320, 375, 768, 1024 and 1440px. Existing workspace isolation/typography checks pass. Local production build, TypeScript/lint and diff checks pass. The incremental-implementation approach keeps the remaining features tracked separately; security checks drove server-side locking, version checks, audit records and recoverable deletion.
- Appointment dialogs now include an explicit customer-notification option for status/time changes, plus an email-current-status action. Contact edits, notes, deletion and restoration do not send messages. Cancellation notifications require using the Cancelled status before deletion. No deployment, push or commit performed.

### Booking communications progress (local only)

- Migration `0007_booking_email_delivery` adds isolated booking sender/templates and a persistent delivery outbox; applied only to verified `localhost:55434/premium_web`. Delivery metadata does not duplicate rendered bodies or recipient snapshots.
- The Booking workspace has Email notifications, with sender/reply-to/admin recipient fields, eight configurable plain-text templates, allowlisted variables, preview, signed-in-admin-only test delivery and paginated delivery history. Website and Course configuration remain separate. SMTP credentials remain server-only.
- Requests enqueue customer/admin messages within the booking transaction. Status/time changes enqueue only when notification is requested. Transport failures never undo a successful booking. Row locking prevents concurrent delivery claims; accepted deliveries are not resent, stale booking versions cannot be retried, and uncertain delivery requires an explicit duplicate-risk confirmation. Retries use current saved templates.
- Verification: template validation tests, PostgreSQL route/outbox tests with a memory-only loopback SMTP sink, missing transport, server disconnect after DATA, accepted/unknown retries, stale booking refusal, settings version conflicts, protected endpoints and recipient restrictions. No email was relayed to real recipients.
- Email editor interaction tests pass at 320, 375, 768, 1024 and 1440px; 320/1440 screenshots reviewed. The 28-test workspace/calendar/public booking/password-recovery regression passed. TypeScript/lint and production build passed. Actual external mail-account sender permission and inbox delivery require environment-specific verification before release.

Rollback: unpublish new events to disable new public requests without destroying history. Roll back the changed code and retain migration data. Only on a disposable database with no new event bookings may the FK, `appointments.booking_event_id` column and `booking_events` table be removed; never drop production history to roll back UI code.

## Implementation references

### Course catalogue progress (local only)

- Migration `0008_course_catalogue` adds subtitle/banner, instructor/category references, skill level, publication state, access/price/currency/subscription metadata and optional enrolment limit. Applied only to verified `localhost:55434/premium_web`. Existing active/inactive courses retain published/archived status; new catalogue entries default to draft. Existing incomplete legacy cards must complete publication fields when edited as published.
- Course administration now has catalogue cards and Add New Course / View / Edit dialogs, with category creation, strict validation, unsaved-change confirmation, native dialog focus containment/Escape dismissal, explicit 44px buttons and locally bundled typography. Scheduled offerings remain separate. The superseded minimal course-create and active-toggle API actions return a helpful conflict instead of bypassing catalogue validation.
- Catalogue writes require active admin authorization, same origin, allowlisted fields, valid instructor/category references and optimistic versions. Duplicates are unpublished copies of catalogue fields only; they do not copy cohorts, registrations or files. Deletion refuses linked offerings/materials; archive stops new public registration without deleting history. Public catalogue responses exclude drafts, archived and private/internal courses.
- Programmes advertises published cards even before a cohort is scheduled. Available-date selection opens the native registration dialog. Successful submission disables duplicate resubmission; network failures remain recoverable. Public registration checks the parent course publication/access state as well as the offering window and stores participants for the administration list.
- Verification: three catalogue validation tests; PostgreSQL CRUD/publication/reference/stale-edit/linked-delete/registration/list tests; signed-in browser creation, reload, archive, public visibility and deletion with database readback. Five-width browser tests cover create/view/edit-discard/duplicate/delete and public registration, including keyboard and width bounds. Screenshots reviewed at 320/1440. Full unit/database suite: 60 passed. Workspace/public access/password recovery regression: 21 passed. No real registration emails sent; temporary fixtures removed.
- Final increment gates: production build, TypeScript/lint and diff checks passed; six catalogue browser tests passed after the final changes. Read-only cleanup checks found zero catalogue/category/instructor fixture rows. Public-card and editor screenshots reviewed at 320/1440px.
- Review limits: enrolment limit is stored but course-wide approval enforcement remains pending F4; offering capacity retains its existing controls. Access/price/subscription fields are metadata without online billing; final protected curriculum access remains pending F2/F3. Category editing/deletion, custom image upload, module/lesson duplication and curriculum editing are not claimed by this increment. Existing registered students retain their access according to the current approval policy. Selected-participant PDFs, analytics/reports, modules/lessons, targeted profile posts and final retained-flow audit remain pending. No fabricated workshop or production deployment.

Rollback: retain new columns and existing course records. Roll back UI/API code if needed; never drop course history, registrations, material files or instructor profiles to revert a layout. The old minimal course-create API fixture was updated to use the catalogue endpoint; the full legacy course-portal E2E suite still requires its final isolated-data/files/email review before running it as a release gate.

### Curriculum and profile learning progress (local only)

- Migration `0009_course_curriculum` stores ordered modules and lessons with publication flags, plain text, HTTPS video URLs or a reference to an existing private material. Applied only to verified `localhost:55434/premium_web`; existing enrolments/files are retained.
- Course administration exposes Modules & lessons with course selection, add/edit/remove/reorder controls, explicit publication, save/reload, unsaved-course-switch confirmation and draft retention while switching internal course tabs. The editor disables course switching during a save. Removal requires confirmation and is applied only on Save curriculum.
- Curriculum writes require active admin access, same origin, strict validation, the current course version and valid active course-wide file references. Replacement is atomic; conflicting identifiers or stale saves leave the existing curriculum intact. Uploaded files are not deleted when lessons are removed. Courses with curriculum must be archived instead of hard-deleted.
- The single-page student profile includes Your course learning. Only the student's own approved/completed enrolments grant access; coordinating another student's enrolment does not. Only published modules and lessons are delivered. Archived materials disappear from download lessons. Text is escaped plain text; video links open only on request in a separate tab, with the external host responsible for its video permissions. Files use authenticated download routes, not public storage paths.
- Verification: 63 unit/database tests passed, including atomic-save/reorder/conflict, draft exclusion, cross-course material rejection, protected download content and revoked approval. A real-session browser test creates isolated admin/student/outsider accounts, uploads a private workbook, saves through the editor, reloads, verifies PostgreSQL persistence, reads on the student profile and proves unrelated/pending students cannot download. Fixture data/files are removed; read-only checks found zero remaining curriculum test courses/profiles/users.
- Editor and profile interaction tests pass at 320, 375, 768, 1024 and 1440px, including locally loaded heading fonts, width bounds, ordering, draft preservation and escaped content. Screenshots reviewed at 320/1440. The 27-test workspace/public-access/password-reset/curriculum regression passed; all six curriculum browser tests passed again after the final layout changes. TypeScript/lint and production build passed.
- Subsequent profile/material work below covers S1/S2. Participant PDFs, analytics/reports, course-wide enrolment-cap enforcement and final full-scope acceptance remain pending. No billing, quizzes, progress tracking, fabricated workshop, deployment, commit or push is introduced.

Rollback: retain the curriculum tables and data while reverting UI/API code. Unpublish modules/lessons to withdraw access without deleting files or enrolments. Never drop live curriculum or course history to revert a presentation change.

### Student profile and administrator updates (local only)

- Migration `0010_student_profile_posts` stores individually addressed plain-text posts with draft/published flags and timestamps. Active course administrators can search student accounts in 25-row pages, create, edit, publish and confirm removal. Stale edits/removals conflict instead of overwriting newer changes. Audit metadata excludes post bodies; posting does not send email.
- Student sign-in opens `/portal/profile`: personal contact details, published admin updates, own enrolments, private materials, invoices/receipts, certificates and modules/lessons, plus sign-out. Students cannot modify admin posts or select another recipient through URL parameters.
- `/api/portal?scope=profile` restricts the page to personal enrolments. The original portal endpoint retains organisation coordinator functions, but never expands personal posts or individually assigned files. Explicit response projections exclude storage keys, internal admin notes and applicant-only data. Invoices remain accessible before approval when needed; learning/material downloads require approved/completed enrolment.
- Verification: real-session admin create/publish/edit/remove, selected-student credential login, profile save and sign-out; another account cannot see the post. Database tests cover draft exclusion, stale versions, inactive accounts, origin/role boundaries, coordinator privacy and student search pagination. Five-width profile/editor tests pass at 320/375/768/1024/1440px; mobile documents and desktop update screenshots reviewed. No actual student records were changed.
- Rollback: retain posts and revert UI/API code, or unpublish posts to withdraw them. Explicitly confirmed post removal deletes that post and cannot be undone from the UI; audit metadata is not a content backup.

### Private course materials (local only)

- Migration `0011_student_material_assignment` adds an optional recipient profile to course materials; applied only to verified `localhost:55434/premium_web`. Existing files remain course/offering-wide. The recipient FK restricts deletion so removing a profile cannot accidentally turn a private file into a shared file.
- Course administration → Materials supports course selection, dependent offering selection and an optional linked student. Uploads retain selections on failure, prevent duplicate submission while pending, and clear after successful persistence. Students need a linked course registration for individual assignment and approved/completed status to download. Coordinators retain shared course access but cannot read another student's individually assigned materials.
- Uploads require active admin and same origin, reject unknown/duplicate metadata, bound the multipart body and file size, match extensions/MIME types, check PDF signatures, UTF-8 text and Office ZIP directory identity, and reject detected VBA/encrypted containers. These checks are not antivirus scanning; no malware-scanning service is claimed.
- Upload/version/archive/audit writes are transactional with a per-assignment/title database lock. Same-title replacement archives earlier versions only in the identical course/offering/student scope. Failed database writes remove only the newly generated provisional file. Restore refuses to coexist with another active version; admins can archive the current version first. Archived files are retained, not deleted.
- Download routes enforce valid identifiers, active accounts, ownership and current approval; missing files return 404. Responses are authenticated attachments with private/no-store and nosniff headers. Student-specific files cannot be attached to a course-wide curriculum lesson. Unscoped legacy material cannot grant non-admin access.
- Verification: 70 unit/database tests passed, including parallel uploads producing versions 1/2/3, isolated archive/restore, cross-student/coordinator denial, missing approval, invalid inputs and rollback cleanup. The final unscoped-download guard passed its targeted integration test. Real signed-in browser upload, reload/database readback, profile download content and archive/restore passed; the existing live curriculum download also passed. Upload UI tests at 320/375/768/1024/1440px cover selection retention and successful retry. The upload/list layout stacks at 1200px to fit alongside the admin sidebar; unrelated forms retain their existing breakpoints. Workspace/public-access/upload regression: 23 passed; profile regressions: 6 passed. Production build and TypeScript/lint passed. Read-only cleanup checks found zero material/curriculum live fixture courses, material test profiles and material/profile live users.
- No external email, production write, deployment, commit or push. Rollback retains the new recipient column and file data; do not drop recipient restrictions or revert to an older download handler that ignores them. Archive individual files to withdraw access safely.

Next acceptance work: retained coordinator/offline-payment flows and final release/security checks. The clarified catalogue-section requirement does not require inventing a specific workshop identity, date, copy, or image.

### Retained attendance and certificate lifecycle (local only)

- Correcting completion clears the completion date, returns completed participants (and a completed parent registration) to approved, and atomically revokes issued certificates with an audit reason. Other participant states are preserved by attendance correction. Whole-registration completion now records participant completion dates; repeated completion retains the original date. Registration status changes away from completed clear completion and revoke affected certificates.
- Certificate issuance requires active administrator access, same origin, a strict participant-only payload and an eligible completed attendee on an uncancelled offering. Issuance and correction share the course/offering lock order, preventing a race from leaving a valid certificate after correction. Repeated/concurrent issue requests return the existing certificate without duplicate audit actions or mail calls. Reissuing after a fresh completion retains the certificate identity and updates its name/course/date snapshot. Existing completed historical certificates are not automatically invalidated merely by cancelling their offering; attendance or registration corrections invalidate them explicitly.
- Protected downloads, the student profile and public verification check current participant status, attendance and the matching completion date as well as revocation. This also rejects inconsistent legacy certificate records. Public verification does not return participant account metadata; downloads remain private/no-store and restricted to the student owner or administrator. Ineligible students no longer see an Issue button. Attendance options now submit canonical values such as `no_show` rather than display labels such as `no show`.
- Certificate PDFs use the existing bundled Sora and Manrope files, embedded subsets, brand colours and centered/wrapped text. Names/titles at current maximum lengths fit within the page; unsupported characters or excessive legacy text produce an explicit error instead of silent loss/clipping. Standard accented-name and maximum-length PDFs were rendered with Poppler and visually reviewed. Generated review artifacts are under `test-results/certificate-preview/`; no document was sent to a real student.
- Seven isolated PostgreSQL tests exercise issuance/correction/reissue, registration completion/cancellation, same-origin and role checks, strict/missing-selection rejection, legacy invalid eligibility, cross-student denial, concurrency and the actual Next admin controls at 375/1440px with reload/readback. Sessions and mail delivery are fixtures; actual role checks, handlers and database operations execute. This is not a claim of external inbox delivery or a new real-session sign-in test. Two PDF tests check embedded fonts, supported accented text, long fields, dates and absence of scripts/attachments. Initial tests reproduced the completion/status and origin bugs; the browser test exposed the missing option-value bug before its fix.
- Final slice gates at that checkpoint: 107 unit/database tests across 32 files, TypeScript/lint, the 80-route build and 22 workspace/password-reset/student-profile browser regressions passed. Certificate fixture schemas remaining: zero. No real student mutation, external mail, deployment, commit or push. Later sections and the acceptance ledger record completion of the broader retained-flow and release checks.

### Course access and offline pricing acceptance (F3, local only)

- Course administration retains Free, One-time fee (offline), Subscription (offline) and Private/internal access, a required numeric price, the explicit JMD/USD/GBP/EUR/CAD currency and subscription information. Free resets the catalogue price to zero. Blank prices stay blank and block saving; extra decimal places are not silently rounded in the input. The reusable course money formatter now displays exact cents and currency codes instead of rounding to whole amounts.
- Subscription notes remain available for editing when access type changes but are omitted from the public API/card unless access is Subscription. This prevents outdated terms from being advertised. Public private-course exclusion applies both to the catalogue and scheduled offerings, and direct public registration for a private offering is rejected.
- Catalogue pricing is descriptive metadata, not a billing engine. Scheduled offerings continue to own their actual registration fee/currency. Creating or editing a catalogue entry does not rewrite existing offering fees, registrations, amount due, participant approval or payment status. Even free/subscription applications require the normal administrative review; no gateway, checkout, charge scheduler or automatic access expiry was added.
- Three isolated PostgreSQL integration tests cover the 4-by-5 access/currency matrix, integer-cent readback, copy/update, strict invalid/missing price/currency and gateway-field rejection, authorization/origin denial, private visibility/direct-registration denial, and unchanged offering/registration values after catalogue edits. Mail is mocked and structure-only temporary schemas are removed; these tests do not change real student data or send external email.
- A separate real-session browser test saves/reloads subscription pricing in CAD, changes to one-time pricing of 0.01, verifies old notes remain in the database but disappear publicly, makes the course private, archives/deletes it and removes its temporary account. Five-width browser tests verify blank/over-precision price rejection, free-price reset, retained notes, saved currency/price and the public offering amount. The new price and stale-subscription checks failed against the original code before the fixes; 320/1440 screenshots were reviewed afterward.
- Final gates for this slice: 98 unit/database tests across 30 files, TypeScript/lint and the 80-route build passed. The 24-test workspace/catalogue/public-access regression passed, followed by six catalogue tests after the monetary formatter change; the real-session test also passed. Read-only cleanup checks report zero course-access fixture schemas and catalogue fixture accounts. No migration, dependency, deployment, commit or push. The incremental implementation skill kept the fixes scoped and individually tested. Full retained-flow/release acceptance remains unchecked above.

### Custom questionnaire and two-step booking acceptance (local only)

- Administrators add/edit/remove text, multiline, choice and checkbox questions. Required/optional controls persist with the event. Public booking uses the selected published event's saved title/subtitle/description, assigned agent image, month/date/time selection, and second-step questionnaire. Name, email, phone and consent remain required. Existing booking records retain their original question labels and answers after an event question is changed or removed.
- Two new structure-only PostgreSQL integration tests verify all answer types, blank required text, overlength values, invalid choices, false required checkboxes, unknown/removed question IDs, persistence and historical-label retention. A real Next browser UI routes into actual handlers and isolated tables: admin adds five questions, reloads, edits one and removes another; the public client completes both steps, retries a connection failure, handles a competing reservation, keeps its answers, selects the next available slot and reaches confirmation with exactly one matching stored request. Authorization and outgoing mail are fixtures in this test; it is not an external email or new real-session claim.
- Five-width editor and public booking tests now exercise all four question types. Screenshot review found an oversized custom checkbox caused by generic text-field styles. A booking-questionnaire-only rule keeps checkboxes at 18px with a 44px-tall clickable label and visible keyboard focus. No global form or breakpoint change was needed. Corrected mobile/desktop screenshots were reviewed; screenshot-only styles hide fixed site chrome and development controls, not application content.
- The complete unit/database suite passed 94 tests across 29 files. Ten responsive editor/booking tests passed, and all five public booking tests passed again with checkbox-size assertions after the CSS correction. TypeScript/lint, diff checks and the 80-route production build passed. No migration, dependency, production data change, external email, deployment, commit or push. Test schemas are removed after verification. The incremental implementation skill kept the database and browser checks separated into testable steps; the remaining ledger requirements stay open.

### Booking availability horizons (local only)

- Event availability supports a count in days, weeks, months or years, or infinite availability. The final date is inclusive, anchored to today in the event's time zone. Months/years preserve the day when possible and clamp to the last day of the target month otherwise. Infinite removes only the end date, not weekly hours, closures, minimum advance notice or occupied-slot protection. This behaviour is explained next to the controls without adding a viewport-specific rule.
- Switching to infinite now resets the unused count to 1, so clearing the number beforehand cannot leave an invalid disabled field that prevents saving. Five-width editor tests cover that regression. Mobile editor capture was reviewed; the existing responsive layout is retained.
- Unit tests cover every unit, local-date boundaries, leap years, multi-month anchors, inclusive final dates, overrides that cannot extend the horizon, daylight-saving gaps/offset changes and UTC collisions. A structure-only isolated PostgreSQL test verifies persistence, final-date acceptance, beyond-horizon rejection, distant infinite availability, and occupied-slot exclusion through actual handlers; outgoing mail is mocked and no real customer email is sent.
- A real-session browser test saves all five units through the editor, reloads, reads PostgreSQL back, and checks actual public availability. Days/months additionally verify the enabled final date and disabled next-month navigation; infinite remains navigable beyond those limits. The test uses a temporary admin/event and never submits a real booking. An abandoned fixture from an earlier interrupted run was identified by its exact generated slug/account, confirmed to have no bookings, and removed. No real event or account was removed.
- Targeted checks: 15 unit/database tests, five responsive editor tests and the real-session horizon test passed. The complete unit/database suite passed 87 tests across 27 files; the workspace/two-step booking regression passed 19 tests. TypeScript/lint and the 80-route production build passed. Fixture cleanup now runs in an afterEach hook with its own timeout budget. No migration, dependency, deployment, commit or push was introduced. The remaining ledger items and release gate are not marked complete.

### Selected-participant PDF reports (local only)

- Course Admin → Reports contains course, offering, participant-status and name/email/organisation filters. Selection is per participant, not per group registration; it persists across 50-row result pages and clears on filter changes. Select-all covers every matching result, with an explicit 1,000-record export limit. Failures retain selection; buttons prevent duplicate submission while generating.
- The dedicated course endpoint requires active client-admin access and same origin, bounds the streamed JSON request to 50KB, validates unique participant IDs and queries current stored fields. Any missing/wrong-course ID rejects the entire export. PDF and error responses are private/no-store; PDFs are attachments with nosniff. Audit records contain course ID/count, not names, email addresses or rendered report bodies.
- Reports have an embedded logo, Sora/Manrope fonts, course title, count, timestamp/timezone, repeated table headers and page numbers. They include selected name/email/offering date/status/attendance/organisation only. Static TTF fonts replace the initial WOFF2 attempt because fontkit failed to subset compound WOFF2 glyphs. Pinned upstream sources, hashes and OFL licenses are retained under `public/fonts/reports`; production Docker already copies `public`. Unsupported characters or oversized fields fail explicitly rather than generating incomplete participant names.
- Verification: 72 unit/database tests passed; the 23-test workspace/public-access/report regression passed; real-session browser tests verify successful UI download, customer/anonymous/inactive-admin denial, origin/body-size/duplicate/unknown-input checks, missing/wrong-course IDs and a deleted participant. Separate PDF.js extraction verified exactly the two selected accented names and absence of private/unselected data, plus 65 unique participant emails across five PDF pages. Rendered PDF pages were reviewed. The final report UI checks cover 320/375/768/1024/1440px, pagination, keyboard selection, error recovery, empty filters, font loading, width bounds and hover contrast. Component-only screenshots hide unrelated fixed site chrome during capture; production chrome is unchanged.
- TypeScript/lint and production build passed. Temporary local fixture courses/accounts were removed; read-only counts returned zero. No real student records, external email, deployment, commit or push. Rollback removes the Reports tab/endpoint and leaves course and participant data intact; no database migration was needed.

### Course analytics and summary reports (local only)

- Course Admin → Analytics provides course and inclusive application-submission date filters in America/Jamaica, defaulting to the current calendar year through today. Each report accepts up to five years. Draft filter changes do not silently change displayed results or download filters; Apply refreshes the snapshot. Empty, loading, validation, failed-load and retry states are explicit.
- Statistics distinguish applications (one per group) from participant enrolment records (not unique people). Approved includes completed. Monthly charts include zero months; status, attendance and application-level payment charts expose exact counts. Course comparison includes archived courses with submissions in the selected interval. Payment statuses are not represented as revenue or outstanding balances.
- The JSON/CSV endpoint requires an active client administrator and strict, bounded filters. Database aggregates run in one read-only repeatable-read transaction without fetching student names, emails or private notes. CSV uses applied filters and a fresh snapshot, includes count definitions, escapes spreadsheet formulas, and downloads as a private/no-store, nosniff attachment. No database migration or additional dependency is required.
- Verification: 77 unit/database tests passed, including grouped counts, date boundaries, zero months, empty/unknown courses and invalid filters. A real-session browser test verifies stored counts, UI download and CSV contents, with anonymous/customer/inactive-admin denial. Five-width tests at 320/375/768/1024/1440px cover keyboard apply, unapplied filters, exact tables, error recovery, empty results, font loading, contrast and width bounds; the combined workspace/public-access/analytics regression passed 23 checks. Desktop/mobile screenshots were reviewed. Charts use internally scrollable plots and exact-value tables on narrow screens; desktop charts switch to two columns at 1400px to fit beside the admin sidebar.
- TypeScript/lint, diff checks and production build (80 routes) passed at this checkpoint. Read-only cleanup counts found zero temporary analytics courses/users/profiles. No real student record, external email, deployment, commit or push was performed. Rollback removes the Analytics tab/endpoint and retains all course data. The final dependency and release result is recorded below.

### Course-wide enrolment controls (local only)

- The catalogue enrolment limit is now enforced across all offerings of that course. It counts approved/completed participant enrolment records, including past offerings; repeated enrolments are records, not deduplicated people. Blank/null means unlimited. Pending applications remain acceptable for review, but a whole group that would exceed the limit is waitlisted without invitations or learning access. Offering hard/soft/unlimited capacity rules are retained; the explicitly labelled offering override cannot bypass the course limit.
- Capacity-changing operations lock course, offering and registration/participant rows in a consistent order. Approvals re-read locked registrations and exclude their own already-counted participants. Concurrent approvals across offerings cannot overbook; catalogue edits use the same course lock and reject a limit below current approved/completed enrolment. Cancelling registrations releases places. Cancelled offerings reject approval/completion. Bulk approvals apply these same rules and return capacity feedback.
- Attendance completion now requires approved/completed participants; completing a whole registration requires all participants to be approved/completed and attended. This closes completion as an alternative path around approval limits. Related mutations require same-origin admin requests. Broader attendance/certificate lifecycle auditing remains part of retained-flow acceptance, not claimed here.
- Verification: 83 tests passed across 26 files, including five enrolment integration tests using structure-only tables in a uniquely named local schema. Tests cover concurrent group approvals, idempotent approval/invitation counts, hard offering capacity, explicit offering overrides, released places, unlimited enrolment, bulk status changes, completed counts, invalid origins/roles, cancelled offerings, and a concurrent limit-reduction/approval race. A real Next UI test routes requests into actual handlers and isolated PostgreSQL tables, verifying capacity feedback and waitlist persistence after reload. Authorization and outgoing mail are test fixtures in that UI test; it is not a new real-session or external-email claim. Catalogue tests additionally verify category references, persisted skill level and bounded limits.
- A stale initial-fetch response could clear dashboard feedback; cancellation handling now ignores abandoned fetches. The final 22-test workspace/catalogue/password-reset browser regression passed, including the five catalogue widths. TypeScript/lint, diff checks and production build passed. Temporary enrolment schemas were removed (read-only count: zero). No real account/enrolment, external email, production write, deployment, commit or push. No migration or dependency added. Rollback should retain the enforcement while numeric limits are configured; do not silently revert to metadata-only limits.

### Offline payment document and added-seat accounting acceptance (local only)

- Invoice/receipt uploads now require an active client administrator and same-origin multipart requests, enforce the existing bounded 25 MB upload reader, reject duplicate/unknown metadata, validate real date-only due dates and integer cents within PostgreSQL bounds, and require matching PDF MIME/extension/signature. These are format checks, not antivirus or full PDF-content validation.
- Registration existence is checked and locked before a provisional file is written. Document metadata and audit history commit in one transaction, while document amount remains evidence metadata only. Uploading an invoice or receipt never changes the registration total or payment status. Failed transactions remove only their generated provisional file; duplicate references return 409. Successful responses omit storage paths and private notes. Storage cleanup failure is reported explicitly rather than silently claiming rollback cleanup succeeded.
- Manual payment-status changes now use strict input and same-origin checks, lock the registration, retain the existing allowed transitions and commit status/history/audit together. Concurrent identical requests do not duplicate history, audit or notification attempts. Notification failure returns a saved-status warning without reverting the payment or inviting a repeat update. Notifications remain after-commit best effort, not a durable outbox; no external inbox delivery is claimed.
- The upload form now accepts exact cents, uses the selected offering currency supplied by the admin snapshot, excludes display-only amount fields from its API payload, prevents duplicate in-flight submissions, retains fields/files after errors and safely clears after success. A failed refresh after successful upload warns against uploading again. Existing bundled Manrope/Sora are retained. Course metric cards fit their available container width using 240px minimum tracks, and course forms adopt the existing 1200px one-column breakpoint so currency totals and the invoice/roster pair do not overflow beside the admin sidebar. The fixes were driven by failing tablet regression checks, not viewport-specific preview attributes.
- Nine isolated PostgreSQL tests cover origin/role/input/file boundaries, forced audit rollback, duplicate references, exact document bytes, student/coordinator ownership, cross-organisation and inactive-account denial, private portal projections, payment transitions, concurrent retries and mail failure. One test drives the actual Next admin upload at 375/1440px into actual handlers and structure-only database tables, then reloads and checks persisted cents/status. Session identities and mail are mocked; no real student or email recipient is used. Temporary schema/file cleanup is verified. The incremental implementation skill kept each failure/fix/test slice bounded.
- The final policy keeps all payment state manual: invoices and receipts are evidence documents, not payment commands. Administrators explicitly update payment status. Coordinator-added seats add the current offering fee to `amountDueCents` in the same transaction as the participant; a paid registration becomes partially paid, while invoiced/refunded registrations return to unpaid. Replacement participants do not change the amount or payment status. Automatic adjustments create payment-history and audit records without charging a gateway. Integer overflow rejects the entire seat addition.
- Final local verification is included in the 169-test whole-build gate. Focused invoice/coordinator tests verify preserved payment state on invoice and receipt uploads, explicit-status preservation, added-seat totals/status/history, overflow rejection, replacement without rebilling, authorization failures and transactional rollback. Invoice forms retain the reviewed responsive layout and explain that uploads do not mark a balance paid. No payment gateway or automatic subscription charging was introduced.

### Organisation roster import acceptance (local only)

- Course administration imports UTF-8 CSV files of 1 byte to 1 MB with 1–250 participants. Headers are name/email and optional phone, in any order; duplicate/unknown headers and mismatched column counts are rejected. The parser preserves quoted commas and escaped quotes, handles BOM/CRLF and normalizes emails. Invalid names/emails/phones, malformed quotes, control characters and duplicate emails reject the entire import with row feedback instead of silently dropping rows. Common browser CSV MIME variants are accepted only for `.csv` files whose UTF-8 content parses and validates. Raw CSV files are not stored.
- The route requires active client-admin access, same origin, unique allowlisted multipart fields and bounded streaming input. It locks the course/offering before checking cancellation, duplicates and the total fee. Pending registration, organisation, participants and audit are transactional; forced audit failure leaves no partial rows. Concurrent duplicate imports return one success and one conflict. Per-offering fee multiplied by actual participant count uses exact cents and rejects out-of-range totals. Imported seats remain pending and unlinked until the existing approval workflow; import does not send invitations/mail, approve seats, reserve capacity or change payment status.
- The form states the file/header limits, excludes cancelled offerings, prevents duplicate in-flight submits and retains inputs/files after errors. Success clears safely after the asynchronous request and reports the participant count. A refresh failure after commit warns against repeating the import. Existing responsive form breakpoints and bundled typography are reused; no new layout framework or dependencies were introduced. The incremental implementation skill kept parser, route and form changes independently tested.
- Three parser tests and five isolated PostgreSQL tests cover quoted fields, invalid rows, bounds/encoding/MIME, roles/origin, malformed metadata, unknown/cancelled offerings, duplicate races, exact totals and audit rollback. The database suite also drives the actual Next form into real handlers and isolated tables at 375/1440px, rejects an invalid file, corrects it and reloads the persisted pending seats. Authentication identities are fixtures; this is not a new real-session or external email claim. Browser fixtures additionally exercise failure/retry at 375/1024/1440px and font loading.
- Final local gates: 124 unit/database tests across 35 files, 28 roster/invoice/material/workspace/password-reset browser regressions, TypeScript/lint and the 80-route build pass. Mobile/desktop form captures were reviewed; font-loading and overflow checks pass. Read-only cleanup found zero roster fixture schemas. No migration, dependency, production write, deployment, commit or push. Rollback retains all stored registrations and existing files; do not reintroduce silent invalid-row dropping.
- At the roster-import checkpoint, coordinator and invitation hardening remained open. Those same-origin/strict-input, row-lock, current-identity, cutoff/cancellation, invitation/certificate invalidation, duplicate, transaction and active-customer checks are completed and verified in the later coordinator and invitation sections. The previously ambiguous workshop item is satisfied by the configurable published catalogue section described in C2.

### Dependency and public-submission security release gate

- Removed unused form packages, moved the shadcn CLI out of production dependencies, upgraded Next.js/ESLint config and Better Auth packages together, and applied two narrow transitive overrides. No forced or blanket audit fix was used. `pnpm audit --prod --json` now reports zero known vulnerabilities across 355 production dependencies.
- Public booking and public course registration reject declared or streamed bodies above their route-specific limits before parsing, distinguish malformed JSON from oversized requests, decode UTF-8 strictly, and apply atomic PostgreSQL fixed-window limits to both global and hashed event/offering-plus-email buckets. Raw customer emails are not stored in rate-limit keys, and untrusted forwarding headers do not select the bucket.
- Migration `0012_salty_celestials` adds the rate-limit store. Migration `0013_empty_power_man` performs a fail-closed Better Auth 1.7 issuer backfill for the configured credential/Google providers, rejects unknown providers or collisions, makes issuer required and replaces the legacy provider/account key with the required issuer/account key. Both were applied only after verifying `localhost:55434/premium_web` as database user `premium`.
- Focused security and concurrency tests pass, the full 169-test suite passes, test schemas are cleaned, and the production build succeeds. The running local review process is bound only to `127.0.0.1:3001`.

### Invitation and account-linking acceptance (local only)

- Invitation acceptance requires an authenticated, same-origin active customer, strict input and a matching current account email. It locks the course/offering, registration and invitation before checking current approval, participant identity/email/ownership, expiry and revocation. Coordinator status is derived from the current organisation applicant rather than trusted from a stale link. Token consumption, participant linking, membership, email verification and audit commit together; an audit failure rolls all of them back. A token can be consumed only once.
- New public registrations link only active customer profiles with a verified email. Approval validates every existing participant/coordinator account before modifying a group, rejecting staff, inactive or missing-profile accounts without partial approval. Verified customers can be linked directly; unverified customers receive the invitation even if they already have an account. Accepting the emailed token records email ownership proof. Existing participant memberships can be upgraded for the current approved coordinator without downgrading another coordinator.
- Approval/rejection decisions invalidate older unused invitations before any new links are issued. Activation restores the submit button after transport errors, retains entered values and reports rejected links. Existing-account activation uses sign-in and then the same protected acceptance endpoint. The two legacy API invitation tests now provide the required same-origin header; that broader legacy suite was not run, and its historical automatic-linking assumptions need review before use.
- Ten isolated PostgreSQL tests cover wrong/disabled/staff accounts, unknown input, malformed JSON, stale participant/registration/offering state, expiry/revocation, concurrent claims, coordinator-role changes, audit rollback (including email verification), account-linking preflight and a public registration through approval/captured invitation to personal-profile access. The actual Next activation form is exercised at 375/1440px against real acceptance, profile and learning handlers, then reloaded. Authentication transport/session identity is mocked in these invitation tests; no claim is made of a full real-auth invitation journey. The existing separate password-recovery tests exercise real Better Auth sessions.
- The 134-test unit/database suite and the 24-test activation/workspace/password-reset/profile browser regression pass. The ten invitation tests were rerun after tightening browser routing to include the actual learning handler and uniquely identifying each activated course. All invitation fixture schemas were removed (read-only count: zero). An early test's uncaptured primary-inbox notification attempted the local SMTP port and was refused; both mail paths are now explicitly mocked, and no external email was delivered. No migration, dependency, real-account change, production write, deployment, commit or push was made in this slice. The incremental-implementation skill kept each security change paired with a failing test and focused verification.
- The production build passes (80 routes). A read-only local-data review found zero linked participant accounts, zero unverified links and zero links to unsuitable profiles; there was therefore no local historical-link migration to perform. Production accounts/data were not inspected or changed.
- Coordinator replacement/review, resend/delivery-failure recovery, lost-success-response navigation and additional-seat accounting are verified in the sections below. New registration/approval protections do not retroactively migrate historical production links; production data was not inspected or changed during this local build.

### Coordinator changes and participant review (local only)

- Add/replace requests use strict participant schemas, same-origin active customer authorization and a current coordinator membership. The operation locks the course, offering, registration, profile/membership and participant before writes. Cancelled/terminal registrations, started offerings, passed substitution cutoffs, completed/cancelled/rejected participants, duplicate emails and stale replacement versions are rejected. A registration is limited to 250 roster entries. A null substitution cutoff permits changes only until the offering starts; it does not allow retrospective replacement.
- Replacement retains the participant identifier, clears the old profile link, attendance, completion and cancellation request, and requires review. Old invitations and certificates are revoked transactionally. The previous participant snapshot is retained in the private audit record rather than exposed through portal responses. Audit failure rolls back replacement and revocations. Replacement does not change fees or payment status. Organisation coordinator membership is independent of personal course enrolment and is not silently revoked when a coordinator replaces their own seat.
- Individual admin status controls now send the participant ID; other group members are unchanged. Re-approving an already-approved group reviews its pending/waitlisted changes instead of returning early or overwriting completed members. Offering and course-wide capacity include retained approved/completed seats. A new seat can be waitlisted or rejected without demoting existing participants; the group status reflects remaining participant states. Other participants' invitations, completion dates and certificates remain intact. Full-group actions remain explicit through registration/bulk requests.
- The roster UI lists only organisations coordinated by the signed-in customer, provides a course-registration selector, labels every field, retains input on failure, resets only after a successful save and includes an explicit refresh control. Replacement submits the displayed version, not a silently refreshed version. The server supplies current change eligibility and rechecks it on write. The roster spans its portal grid; fields stack at the existing 640px compact breakpoint. Locally bundled Manrope is retained. Mobile and desktop captures were reviewed; 375/1024/1440px overflow and interaction checks pass.
- Six isolated PostgreSQL tests cover role/origin/input/version denials, terminal states/cutoffs, concurrent replacements, inherited-record clearing, former-owner removal, certificate/invitation revocation, audit rollback, capacity-aware approval/rejection, unchanged completed members and unchanged paid balances. One uses the actual Next roster and admin controls against actual handlers/database at 375/1440px, including reloads. Session identity and outbound mail are fixtures; no real account or external email was used. Three additional Playwright tests cover selecting the correct registration, failed-save retry, field reset, version submission, membership filtering and responsive layout.
- The 140-test unit/database suite and 27-test coordinator/workspace/activation/profile/password-reset browser regression passed at this checkpoint. An invitation browser test's default one-second Vitest polling timeout was too short under concurrent full-suite load; its unchanged assertions now use the same ten-second bound as the other browser-backed integration tests. Additional-seat pricing was completed later: a new seat adds the current offering fee transactionally and conservatively adjusts manual payment status, while replacement remains non-billable.
- No migration, dependency, production data change, deployment, commit or push was introduced. The incremental-implementation skill kept each lifecycle change paired with failing and passing tests. Rollback must retain the old-participant audit history and current revocations; do not restore a UI that omits the replacement version or silently reapproves completed group members.
- Final checks for this slice: the 80-route production build and diff checks pass; read-only cleanup found zero coordinator or invitation fixture schemas remaining.

### Access email recovery and decision delivery (local only)

- Registration approval and other review decisions now return notification attempt/failure counts and an explicit saved-decision warning when mail returns undelivered or throws. One failed recipient does not prevent subsequent recipients being attempted. Repeating an already-approved, unchanged registration does not send mail or repeat the approval. The existing admin notice displays the warning after reloading saved data; it directs administrators not to repeat a decision as an email retry.
- A separate Course access email form selects an approved student or an organisation coordinator from current registration data. Its protected endpoint derives the destination and role on the server; it rejects arbitrary email/role fields, other-group participants, pending/rejected registrations or participants, cancelled offerings, unsuitable accounts and conflicting profile links. Course/offering/registration locks serialize changes with acceptance and replacement. Audit-backed per-recipient cooldown persists for one minute. Old unused recipient invitations are revoked before a fresh hashed seven-day invitation is created; a verified, correctly linked account gets a login link instead. No enrolment, payment, attendance or decision changes occur. Audit failure rolls back token rotation. Failed mail is explicit and can be retried after the cooldown.
- The form retains its selected recipient after failure, prevents duplicate in-flight submissions and uses existing responsive card styles and bundled Manrope. Actual Next forms at 375/1024/1440px call actual handlers against isolated PostgreSQL fixtures: approval warning, failed resend, cooldown, successful retry, coordinator delivery and persisted approval after reload are covered. Desktop capture was reviewed; the mobile element capture includes the sticky workspace header over its upper area, so it is not treated as a clean full-card visual proof. Form overflow and controls pass at all three widths.
- The activation page now offers explicit sign-in and forgotten-password links after an interrupted or already-used activation, plus guidance to request a new invitation. It does not relax one-use token checks or grant access from an invalid link. Responsive activation tests verify the recovery links remain available after errors.
- Six added database-backed tests cover the new decision-delivery and resend paths, including concurrent one-success/one-cooldown requests, protected destinations, acceptance of the fresh token, rejection of the old token, verified-account login links, coordinator acceptance, transport failure and forced audit rollback. The full 146-test checkpoint, its browser regressions and TypeScript/lint passed. The later real-auth, financial and release sections close those technical gates; no real student account, external recipient, production data, deployment, commit or push was changed.

- Final production build passes with 81 routes, including the new protected access-email endpoint. Incremental implementation kept the approval-reporting fix, resend endpoint and admin form independently checked before the full regression run. No release or deployment is implied by a local production build.

### Real-session invitation journey verification (local only)

- `src/server/course-invitations-live.integration.test.ts` substitutes only the database connection, directing the real Better Auth implementation and course handlers to uniquely named, structure-only local PostgreSQL tables. No session or email transport is mocked. The SMTP receiver is bound to an ephemeral `127.0.0.1` port, permits only `example.test` recipients and stores messages in memory. The local database hostname, port, database name and schema prefix are checked before fixture creation; only the generated schema is removed afterward. No public auth or registration rows are modified.
- The API journey submits a real public organisation registration, checks two receipt messages, approves using a genuine administrator session, checks separate student/coordinator invitation messages, creates real customer accounts and accepts each token. Anonymous and administrator identities cannot consume those tokens. Acceptance verifies email ownership; a second use fails. The student sees personal learning enrolment while a non-participant coordinator sees the managed registration but no personal course learning.
- At 375/1440px, the actual Next activation and login forms call the real Better Auth HTTP handler, with response cookies handled by the browser, and real invitation/profile/learning handlers. Requests are bridged into the isolated test database; this is not an unmodified production-server or external-inbox test. Only the read-only website-shell content request uses the running local server normally. The journey covers a new signup and an existing unverified account, a fresh invitation from the resend endpoint, visible published lesson content, email verification in PostgreSQL and profile reload. At 1440px the test discards the successful activation response, then uses the page's sign-in recovery link and existing password to reach the profile; the token was accepted exactly once.
- Initial failures were fixture defects: UUID/text parameter casts, quoted-printable email parsing, a public shell API allowlist omission, and a lesson locator that ignored the numbered summary. They were corrected without changing application logic or relaxing the access assertions. The new two-test suite and the 148-test checkpoint passed. Financial and final security/release gates were completed afterward; workshop content remains external.

### Website inbox boundary verification (local only)

- Website inbox reads explicitly use private/no-store caching and continue to exclude booking submissions. Updates require a same-origin administrator and strict ID/status/displayed-version input; malformed JSON and unknown fields are rejected. Missing or excluded submissions return 404 instead of a false success. A row lock and timestamp comparison reject a competing stale review with 409. Status and a metadata-only audit event commit together, and an audit failure rolls back the status change. Repeating the current status does not create a second audit. No booking/course records or submission payloads are rewritten.
- The inbox sends its displayed version, disables controls while a request is in flight, applies the returned saved record, reports failures and retains the prior state until a successful save or explicit refresh. A refresh failure keeps existing records visible. The new refresh control reuses the existing admin toolbar styling; form controls retain bundled Manrope. Old browser clients must reload to obtain/send `updatedAt` before changing status.
- Four added isolated PostgreSQL tests cover private reads, booking exclusion, authorization/origin/strict input, malformed JSON, missing IDs, competing reviews, audit rollback, and actual Next inbox interaction at 375/1024/1440px. Browser checks exercise failed save, stale review, failed refresh, successful recovery and persistence after reload. Mobile/desktop screenshots were reviewed; overflow and font checks pass. Session identity is a fixture in this suite; broader real-session workspace authorization remains covered separately.
- The full suite passed 152 tests across 38 files at this checkpoint. The inbox browser fixture was later made deterministic under development-mode duplicate initial reads by injecting its simulated refresh outage only after the explicit user action. No real-account mutation, external email, deployment, commit or push was introduced. The later whole-build gate passes 169 tests; workshop content remains external.

- Final inbox slice checks: TypeScript/lint and the 81-route production build pass. Read-only cleanup found zero Website inbox or real-auth invitation fixture schemas remaining. Diff checks pass. The final mobile toolbar capture was reviewed after the full-suite rerun.

### Student password recovery verification

Course administration now includes a student selector and an explicit send-reset-email action. The endpoint requires an active client administrator, checks same-origin requests, resolves an active customer account from its registration on the server, applies a database-backed one-minute per-account cooldown, and records an audit request without passwords or tokens. Reset links expire after 30 minutes; successful resets revoke existing sessions. Delivery acknowledgement is request-local because Better Auth absorbs public recovery callback errors.

Verification: 10 route unit tests and 3 real Better Auth flow tests using isolated in-memory accounts and captured mail, plus desktop/mobile fixture browser tests and a real unauthenticated-endpoint check. An additional real-session integration test clones only table structures into a uniquely named local PostgreSQL schema and captures real Nodemailer SMTP traffic on 127.0.0.1. It verifies unauthenticated/customer/inactive-admin denial, inactive/staff student exclusion, same-origin/input checks, missing SMTP, concurrent requests producing one message and one audit record, the one-minute persisted cooldown, 30-minute expiry, single use, new-password login and session revocation. The temporary schema is removed afterward; no actual account or production data is changed. External inbox delivery is not claimed. The final whole-build suite verifies this together with the other course/profile and booking requirements.

Installed Next.js routing documentation was inspected before routing changes; the final verified build uses Next.js 16.3.4:
- https://nextjs.org/docs/app/api-reference/functions/redirect
- https://nextjs.org/docs/app/getting-started/route-handlers

Shared UI remains on locally bundled font packages; existing administrator authorization is retained. No new third-party integrations are introduced.
